# dotmemo

## Developing

On Linux/macOS:

```
python -m venv .venv
source ./.venv/bin/activate
python -m pip install -r ./requirements.txt
```

On Windows:

```
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r .\requirements.txt
```
